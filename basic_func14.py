# Create a function called main.
# Create function arguments a and b.
# Returns the floor division of a and b.
from math import floor


def main(a,b):
    from math import floor
    return floor(a), floor(b)
print(main(-9.12, -15.9))
